<link rel="stylesheet" href="/public/styles.css">
<ul class="item-container">
	<li class="menu-item">
		<h4>{{model.name}}</h4>
		<img src="/img/{{model.image}}" alt="" width="300" height="400">
		<p>{{model.rating}}</p>
		<p>
			{{model.description}}
		</p>
		<p>
			<a href="/item/{{model.name}}/nutrition" class="link">Nutrition Info</a>
		</p>
	</li>
</ul>
{{outlet}}